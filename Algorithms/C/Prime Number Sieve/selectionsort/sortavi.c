selectionSort(array,size)
repeat(size-1)times
set the first unsorted elements as the minimum
for each of the unsorted elements
if element < currentMinimum
set element as new minimum
swap minimum with unsorted position
end selectionSort